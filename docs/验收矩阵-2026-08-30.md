# 验收矩阵（2026-08-30 现场验收）

定位改动：工具条默认出现在选区**上方**（底边距光标行 8px），上方空间不足时翻到光标下方（dip.y+18），左右贴边翻转保留。

## A. 定位复验（附录 X.10 简化版）

| # | 场景 | 预期 | 结果 |
|---|------|------|------|
| A1 | 屏幕中部划词 | 工具条在选区上方，不遮选区 | ✅（曾被豆包工具条叠住，禁用后判定不受影响） |
| A2 | 窗口拖到屏幕顶部再划词 | 上方无空间 → 翻到下方 | ✅ |
| A3 | 屏幕右缘划词 | 工具条左移不超出屏幕 | ✅ |
| A4 | 结果态（长回复） | 结果展开、窗口向下生长不越界；点外部隐藏 | ✅（修复流式闪烁：动画仅状态切换播一次 + 高度上报 150ms 防抖） |
| A5 | 缩放显示（100%/125%）下贴光标 | 无明显偏移 | ☐ |

## B. 取词矩阵（各 ≥10 段，命中 ≥90%）

| 应用 | 通道 | 命中 | 备注 |
|------|------|------|------|
| Chrome | UIA | ✅ | |
| Edge | 剪贴板回退 | ✅ | UIA 未命中该页面，回退正常；注意剪贴板副作用（C2 重点验证） |
| 记事本 | UIA | ✅ | |
| VSCode | 剪贴板回退 | ✅ | Code.exe |
| WPS | 剪贴板回退 | ✅ | **修复**：WPS 多进程（锁定=主进程 pid，焦点元素=子进程 pid）。新增 IsTrustedFocus：窗口根属主相同（GetAncestor GA_ROOT）即信任；次选同名进程/共同祖先。Word/Excel/PDF 三形态实测通过 |
| Typora | 剪贴板回退 | ✅ | |

## C. 安全与隐私

| # | 场景 | 预期 | 结果 |
|---|------|------|------|
| C1 | 密码框划词 | 0 触发（UNKNOWN=BLOCKED） | ✅ |
| C2 | 剪贴板还原 | 选词后原剪贴板内容还在 | ✅ |

## D. 视觉模型（bigmodel glm-4v-flash，OpenAI 兼容适配器）

| # | 场景 | 预期 | 结果 |
|---|------|------|------|
| D1 | 聊天窗粘贴截图 + 视觉模型 | 图片上传、模型识别内容 | ✅（glm-4v-flash 正确识别截图人物；OpenAI image_url 格式兼容） |
| D2 | 非视觉模型（glm-4-flash 自动选择）同操作 | 明确报错/提示，不挂死 | ✅（API 报"content.type 参数非法"并显示在气泡中） |

## 结论

- A/B/C/D 全部通过（A5 缩放显示复测可选项）。
- 当日修复：① 工具条默认方位改选区上方+翻转；② 流式闪烁（动画重放+高度抖动）；③ WPS 多进程取词（IsTrustedFocus 窗口根属主判定）；④ 会话列表不刷新（focus 重拉）；⑤ 聊天窗可拖动（手动拖拽替代 app-region）；⑥ i18n 补 cancel/retry/processing 键；⑦ 四语种新增（zh-CN/zh-TW/ru/ar + RTL）；⑧ UI 四件套统一主题 + 图标体系；⑨ 开机自启默认改为关。
