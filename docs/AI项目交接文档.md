# 项目交接文档

> 你现在要为当前项目生成一份"超级详细的项目交接文档"。
>
> 这份文档的目标是：让我在一个全新的 AI 聊天窗口里，只需要粘贴这份文档，新 AI 就能无缝接手继续开发。
>
> 请不要写空泛总结。
>
> 你必须基于当前上下文，尽可能详细、准确、结构化地整理项目状态。
>
> 如果有不确定的信息，请明确标注"不确定"，不要编造。
>
> 请按以下结构输出：

---

## 1. 项目基本信息

请说明：

- **项目名称**：
- **项目目标**：
- **这个项目最终要解决什么问题**：
- **当前开发阶段**：
- **技术栈**：
- **运行环境**：
- **主要依赖**：
- **项目所在目录**：
- **当前分支或版本状态**：
- **是否有线上部署**：
- **是否涉及数据库**：
- **是否涉及第三方 API**：
- **是否涉及敏感配置或密钥**：

> **注意**：
> 如果项目里有 API Key、Token、密码、私钥、Cookie，请不要输出真实值。
> 统一写成：`[REDACTED]`

---

## 2. 当前项目进度

请详细说明：

- 已经完成了哪些功能？
- 每个功能对应哪些文件？
- 哪些功能已经验证可用？
- 哪些功能只是写了代码但还没验证？
- 哪些功能写到一半？
- 哪些需求后来被放弃了？
- 哪些方案已经证明不可行？
- 哪些地方不要让新 AI 重复尝试？

请按模块列出来。

**格式示例：**

### 模块 A：xxx

- **状态**：已完成 / 开发中 / 未验证 / 已废弃
- **相关文件**：
- **核心逻辑**：
- **当前问题**：
- **下一步**：

---

## 3. 文件结构说明

请输出当前项目关键文件结构。
不要只列文件名。
每个重要文件都要说明它的作用。

**格式：**

- `path/to/file`
  - **作用**：
  - **当前状态**：
  - **里面最重要的函数/类**：
  - **新 AI 修改时要注意什么**：

请重点说明：

- 入口文件
- 配置文件
- 核心业务逻辑文件
- API 路由文件
- 数据库相关文件
- 前端页面文件
- 工具函数文件
- 测试文件
- 部署相关文件

---

## 4. 核心逻辑说明

请用通俗语言解释当前项目的核心运行流程。

包括：

- 用户/请求从哪里进入？
- 数据经过哪些步骤处理？
- 哪些函数负责关键逻辑？
- 哪些地方容易出 bug？
- 哪些逻辑已经改过？
- 为什么要这样设计？
- 有没有临时方案或技术债？

请不要只贴代码。
我要的是"新 AI 能理解项目为什么这样写"。

---

## 5. 环境变量与配置

请列出项目需要的环境变量和配置项。

**格式：**

- `ENV_NAME`
  - **用途**：
  - **是否必填**：
  - **示例值**：不要写真实密钥，只写格式示例
  - **缺失会导致什么问题**：

请检查是否存在：

- `.env`
- `.env.example`
- `config`
- `settings`
- 数据库连接配置
- API 配置
- 代理配置
- 部署配置

如果你不确定某个配置，请标注"不确定"。

---

## 6. 启动、运行、测试方式

请告诉新 AI 如何启动这个项目。

包括：

- 安装依赖命令
- 本地启动命令
- 后端启动命令
- 前端启动命令
- 数据库启动方式
- 测试命令
- 构建命令
- 部署命令
- 常见启动失败原因
- 启动成功后应该看到什么

请按步骤写。

如果某个命令已经测试过，请标注：**已验证**。
如果只是推测，请标注：**未验证**。

---

## 7. 已知问题与坑

请列出当前项目所有已知问题。

包括：

- 还没修的 bug
- 偶发错误
- 性能问题
- 兼容性问题
- 类型错误
- 构建错误
- 部署问题
- 数据异常
- 权限问题
- 第三方 API 问题
- 之前踩过但已经修好的坑

每个问题按这个格式写：

### 问题：xxx

- **表现**：
- **可能原因**：
- **已尝试方案**：
- **有效方案**：
- **无效方案**：
- **相关文件**：
- **新 AI 下一步该怎么查**：

---

## 8. 最近修改记录

请按时间线整理最近重要修改。

**格式：**

### 修改 1：xxx

- **修改原因**：
- **修改文件**：
- **改了什么**：
- **为什么这么改**：
- **是否验证**：
- **可能影响**：

请重点写：

- 最近新增的功能
- 最近修复的 bug
- 最近重构的逻辑
- 最近改动过但风险较高的地方
- 最近用户确认过的需求
- 最近废弃掉的方案

---

## 9. 下一步开发计划

请给新 AI 一个明确的继续开发路线。
不要写"继续完善项目"这种废话。

### 下一步最优先做什么？

- **目标**：
- **原因**：
- **涉及文件**：
- **具体步骤**：
- **验收标准**：

### 第二优先级

- **目标**：
- **原因**：
- **具体步骤**：

### 第三优先级

- **目标**：
- **原因**：
- **具体步骤**：

同时说明：

- 哪些任务不要现在做？
- 哪些功能容易过度开发？
- 哪些地方要先验证再写代码？
